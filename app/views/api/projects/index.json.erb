[
    <% @projects.each do |project| %>
       {
            name: "<%= project.name %>",
            created_at: "<%= project.created_at.strftime("%b %d, %Y") %>"
       }<%= "," unless project == @projects.last %>
    <% end %>
]